name: Licence audit

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "23 8 * * 1"

jobs:
  audit:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - run: brew install ripgrep
      - run: make licence-audit
