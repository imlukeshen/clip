# ConvertKit

`ConvertKit` owns the pure conversion decision table and the remux,
VideoToolbox, ImageIO, and linked-FFmpeg execution paths.

It must never import application UI or AI packages and must never spawn an
FFmpeg executable.
