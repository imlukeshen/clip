# LibraryStore

`LibraryStore` owns Clip's on-disk library layout, security-scoped bookmarks,
project packages, immutable asset metadata sidecars, and rebuildable SQLite
index. It may depend only on CoreModel, Foundation, and GRDB.
